import {render,screen,waitFor} from "@testing-library/react";import {beforeEach,describe,expect,it,vi} from "vitest";import LoginPage from "@/app/login/page";
const replace=vi.fn();vi.mock("next/navigation",()=>({useRouter:()=>({replace})}));vi.mock("@/lib/auth",async original=>{const actual=await original<typeof import("@/lib/auth")>();return {...actual,fetchCurrentUser:vi.fn().mockResolvedValue({id:1})}});
describe("authentication and validation",()=>{beforeEach(()=>replace.mockClear());it("redirects an authenticated user away from login",async()=>{render(<LoginPage/>);await waitFor(()=>expect(replace).toHaveBeenCalledWith("/dashboard"))});it("marks both login fields as required and email-shaped",()=>{render(<LoginPage/>);const email=screen.getByLabelText("Email");expect(email).toBeRequired();expect(email).toHaveAttribute("type","email");expect(screen.getByLabelText("Password")).toBeRequired()})});

"use client";
import {FormEvent,useEffect,useState} from "react";import {useRouter} from "next/navigation";import {AppShell} from "@/components/AppShell";import {get,post} from "@/lib/api";import type {CurrentUser} from "@/lib/auth";import type {TicketDetail} from "@/lib/types";
type Ref={id:number;key?:string;name:string};
export default function NewTicketPage(){return <AppShell require="TICKET_CREATE">{user=><Form user={user}/>}</AppShell>}
function Form({user}:{user:CurrentUser}){const router=useRouter();const [orgs,setOrgs]=useState<Ref[]>([]),[types,setTypes]=useState<Ref[]>([]),[org,setOrg]=useState(user.organizationId?String(user.organizationId):"");const [type,setType]=useState(""),[title,setTitle]=useState(""),[description,setDescription]=useState(""),[priority,setPriority]=useState("MEDIUM"),[severity,setSeverity]=useState("SEV_3"),[error,setError]=useState("");useEffect(()=>{if(user.party==="TICKETFLOW1")get<Ref[]>("/reference/organizations").then(setOrgs);},[user.party]);useEffect(()=>{if(org)get<Ref[]>(`/reference/ticket-types?organizationId=${org}`).then(values=>{setTypes(values);setType(values[0]?.key??"")});},[org]);async function submit(e:FormEvent){e.preventDefault();setError("");try{const ticket=await post<TicketDetail>("/tickets",{type,title,description,priority,severity:type==="DEFECT"?severity:null,organizationId:user.party==="TICKETFLOW1"?Number(org):null});router.push(`/tickets/${ticket.ticketKey}`)}catch(e){setError(e instanceof Error?e.message:"Could not create ticket.")}}return <div className="max-w-2xl"><h1 className="text-3xl font-bold">Create ticket</h1><form className="card mt-6 space-y-4" onSubmit={submit}>{user.party==="TICKETFLOW1"?<Select label="Organization" value={org} set={setOrg} options={orgs.map(o=>[String(o.id),o.name])}/>:null}<Select label="Ticket type" value={type} set={setType} options={types.map(t=>[t.key??"",t.name])}/><label className="block"><span>Title</span><input className="field mt-1" required maxLength={300} value={title} onChange={e=>setTitle(e.target.value)}/></label><label className="block"><span>Description</span><textarea className="field mt-1 min-h-32" required value={description} onChange={e=>setDescription(e.target.value)}/></label><Select label="Priority" value={priority} set={setPriority} options={["LOW","MEDIUM","HIGH","CRITICAL"].map(v=>[v,v])}/>{type==="DEFECT"?<Select label="Severity" value={severity} set={setSeverity} options={["SEV_1","SEV_2","SEV_3","SEV_4"].map(v=>[v,v])}/>:null}{error?<p role="alert" className="text-red-700">{error}</p>:null}<button className="btn-primary">Create ticket</button></form></div>}
function Select({label,value,set,options}:{label:string;value:string;set:(v:string)=>void;options:string[][]}){return <label className="block"><span>{label}</span><select className="field mt-1" required value={value} onChange={e=>set(e.target.value)}><option value="">Select…</option>{options.map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></label>}
